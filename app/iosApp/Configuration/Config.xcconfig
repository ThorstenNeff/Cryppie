TEAM_ID=8576C355B9

PRODUCT_NAME=Cyppie
PRODUCT_BUNDLE_IDENTIFIER=com.tneff.cyppie

CURRENT_PROJECT_VERSION=1
MARKETING_VERSION=1.0

// WalletConnect/Reown projectId (KAN-131). Default empty; the real value comes from the gitignored
// Config.local.xcconfig generated by `./gradlew generateWcIosConfig` (single source = `wcProjectId` in
// ~/.gradle/gradle.properties). Never committed. Info.plist exposes it via $(WC_PROJECT_ID).
WC_PROJECT_ID =
#include? "Config.local.xcconfig"
